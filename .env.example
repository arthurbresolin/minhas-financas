# URL da API. Em dev na mesma máquina, a porta padrão do backend é 8000 —
# aqui está 8001 porque a 8000 já é usada por outro projeto nesta máquina.
# No celular (Expo Go) isto tem que ser a URL do túnel, não localhost.
EXPO_PUBLIC_API_URL=http://localhost:8001
